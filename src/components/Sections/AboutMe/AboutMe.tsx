import React from 'react'
import SectionContainer from '../../../containers/SectionContainer'

const AboutMe: React.FC = () => {
  return <SectionContainer>AboutMe</SectionContainer>
}

export default AboutMe
